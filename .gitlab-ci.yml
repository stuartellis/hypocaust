image: docker:20.10.16

variables:
  DOCKER_TLS_CERTDIR: "/certs"

services:
  - docker:20.10.16-dind

before_script:
  - apk add bash make

stages:
  - plan

plan:
  stage: plan
  script:
    make tf:init
    make tf:plan
